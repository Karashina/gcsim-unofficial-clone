# Production Environment Configuration for gcsim-webui
# Copy this file to /etc/gcsim-webui/.env on the server and fill in the values

# JWT Authentication Secret (REQUIRED)
# Generate with: openssl rand -base64 48
# Minimum 32 characters required
GCSIM_JWT_SECRET=

# NOTE: Initial Admin User Credentials (Hardcoded)
# The initial admin user is created automatically on first startup:
#   Username: karashina
#   Password: TestPass1234! (MUST be changed after first login)
#   Email: karashina@gcsim.local
# If an admin user already exists, no new admin will be created.

# CORS Configuration (REQUIRED for production)
# Comma-separated list of allowed origins (no spaces)
# Example: https://gcsim-uoc.linole.net,https://example.com
# Use "*" only for development/testing (NOT recommended for production)
GCSIM_CORS_ALLOWED_ORIGINS=https://gcsim-uoc.linole.net

# Trusted Proxy Configuration (REQUIRED when behind Cloudflare Tunnel or reverse proxy)
# Comma-separated list of trusted proxy IP addresses (no spaces)
# For Cloudflare Tunnel running locally: use 127.0.0.1
# For nginx reverse proxy: use the nginx server's internal IP
# Example: 127.0.0.1,192.168.1.233
GCSIM_TRUSTED_PROXIES=127.0.0.1

# Database Path (Optional - defaults to ./data/gcsim.db)
# Ensure the directory exists and is writable by www-data user
# GCSIM_DB_PATH=/var/www/html/data/gcsim.db

# Server Port (Optional - defaults to :8382)
# Note: systemd unit file may override this with -addr flag
# GCSIM_PORT=:8382
