# webui-src

This folder contains the source used to build the `webui` distribution.

- Run `npm install` here, then `npm run build` to produce `../webui/app.js`.
- Replace `src/app.js` with the original UI source when available.
